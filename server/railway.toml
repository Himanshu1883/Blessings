[build]
buildCommand = "mkdir -p seed-assets && cp -r ../src/assets/. seed-assets/ && npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
