# Development browser proxy. Production browser uses same-origin /api via vercel.json.
VITE_API_URL=http://localhost:4000

# SSR (Vercel/server): live Express API. If unset in production, SSR uses VERCEL_URL or Railway.
# API_URL=https://blessings-production.up.railway.app
